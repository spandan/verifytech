version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: backend-api/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - backend-api/.env
    environment:
      - PUBLIC_BASE_URL=http://localhost:3000
      - CORS_ORIGINS=http://localhost:3000
    volumes:
      - ./backend-api:/app/backend-api
      - ./packages:/app/packages

  frontend-public:
    build:
      context: ./frontend-public
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - api

  # frontend-dashboard is on hold — uncomment when resuming work
  # frontend-dashboard:
  #   build:
  #     context: ./frontend-dashboard
  #   ports:
  #     - "3001:3000"
  #   environment:
  #     - NEXT_PUBLIC_API_URL=http://localhost:8000
  #     - NEXT_PUBLIC_PUBLIC_SITE_URL=http://localhost:3000
  #   depends_on:
  #     - api
